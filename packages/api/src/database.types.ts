/**
 * STUB. Este archivo se regenera con:
 *   npm run db:types
 * (requiere `supabase start` corriendo localmente). No editar a mano:
 * el próximo `db:types` lo sobrescribe con los tipos reales del esquema
 * en supabase/migrations/.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
